version: 2


jobs:
  build:
    docker:
      - image: docker:17.05.0-ce-git
    working_directory: ~/repo
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Build application Docker image
          command: docker build -t nicolas2lee-tutorial/quick-start-java-springboot:latest .
workflows:
  version: 2
  build:
    jobs:
      - build
